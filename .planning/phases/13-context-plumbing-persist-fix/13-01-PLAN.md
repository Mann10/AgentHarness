---
phase: 13-context-plumbing-persist-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [context/message.py, tests/test_persist.py]
autonomous: true
requirements: [ACT-03]
must_haves:
  truths:
    - "A loaded skill body added as a system-role message persists in session memory and stays visible to the model on every turn"
    - "Existing stored sessions load unchanged — persist defaults to True for any message read from the JSONL file"
  artifacts:
    - path: "context/message.py"
      provides: "Message.persist flag — the datum-level contract for skill-body exemptness"
      contains: "persist"
    - path: "tests/test_persist.py"
      provides: "Contract tests for the persist flag (default True, from_dict compat, to_llm_messages visibility)"
      min_lines: 40
  key_links:
    - from: "context/message.py"
      to: "context/context.py"
      via: "add_message appends Message to _messages; to_llm_messages renders it"
      pattern: "persist"
---

<objective>
Add the `Message.persist` field — the datum-level contract that lets loaded skill bodies live in context memory while being exempt from JSONL serialization (ACT-03).

Purpose: This is the foundation of the Phase 13 persist plumbing. A `persist: bool = True` default means every existing caller (agent turns, store round-trips, summarization) keeps working unchanged; only skill-body injection (Phase 14's `add_skill_message()`) constructs `persist=False` messages. Backward compatibility is proven, not assumed.
Output: `Message.persist` field in `context/message.py` + `tests/test_persist.py` (new test file — the Wave 0 gap for this phase).
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-context-plumbing-persist-fix/13-RESEARCH.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from the codebase — use directly, no exploration needed. -->

From context/message.py (current, 33 lines):
```python
@dataclass
class Message:
    role: str
    content: str
    token_count: int = 0
    tool_calls: list[ToolCall] | None = None
    tool_call_id: str | None = None

    def __post_init__(self) -> None:
        if self.role not in VALID_ROLES:
            raise ValueError(...)

    @classmethod
    def from_dict(cls, d: dict) -> Message:
        tool_calls = None
        if d.get("tool_calls"):
            tool_calls = [ToolCall(**tc) for tc in d["tool_calls"]]
        return cls(role=d["role"], content=d.get("content", ""),
                   token_count=d.get("token_count", 0),
                   tool_calls=tool_calls, tool_call_id=d.get("tool_call_id"))
```

From context/context.py — the message lifecycle the persist flag must not disturb:
```python
async def add_message(self, message: Message) -> None:  # L29 — appends, counts tokens
def to_llm_messages(self) -> list[dict]:                # L57 — renders ALL messages for the model
async def _maybe_summarize(self) -> None:               # L75 — exempts role == "system" (L88)
```
</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: RED — write contract tests for Message.persist</name>
  <files>tests/test_persist.py</files>
  <read_first>tests/test_persist.py (if exists — else create), tests/test_store.py (fixture patterns), context/message.py (current Message contract)</read_first>
  <acceptance_criteria>
    - tests/test_persist.py exists and contains 4 tests: default persist, explicit persist=False, from_dict compat, to_llm_messages visibility
    - `python -m pytest tests/test_persist.py -x` fails with AttributeError on the persist attribute (RED state)
  </acceptance_criteria>
  <behavior>
    - Test 1: `Message(role="user", content="hi").persist is True` — default is persist (backward compat)
    - Test 2: `Message(role="system", content="body", persist=False)` — explicit opt-out accepted; `persist is False`
    - Test 3: `Message.from_dict({"role": "user", "content": "x", "token_count": 1}).persist is True` — stored events (which never carry the key) default to persist
    - Test 4: A `persist=False` system message added via `ConversationContext.add_message()` still appears in `to_llm_messages()` — the model keeps seeing it every turn (ACT-03)
  </behavior>
  <action>Create `tests/test_persist.py` with the four tests above (pytest async style, `asyncio_mode = auto`). Test 4 builds a `ConversationContext(count_tokens=len, token_limit=1000)`, calls `await ctx.add_message(Message(role="system", content="SKILL BODY", persist=False))`, then asserts `"SKILL BODY" in [m["content"] for m in ctx.to_llm_messages()]`. Run the file — it MUST fail on the `persist` attribute (feature absent). This is the Wave 0 scaffold for the phase: every later plan appends tests here.</action>
  <verify>
    <automated>python -m pytest tests/test_persist.py -x --collect-only; then python -m pytest tests/test_persist.py -x (expect RED: AttributeError: 'Message' object has no attribute 'persist')</automated>
  </verify>
  <done>The four contract tests exist in `tests/test_persist.py` and fail for the correct reason (persist attribute missing)</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: GREEN — implement Message.persist field</name>
  <files>context/message.py</files>
  <read_first>context/message.py (file being modified — see current state in plan context), tests/test_persist.py (tests to satisfy)</read_first>
  <acceptance_criteria>
    - `context/message.py` contains `persist: bool = True` (last dataclass field, after tool_call_id)
    - `Message.from_dict` does NOT read a `persist` key (stored events default to True)
    - `python -m pytest tests/test_persist.py -x` passes all 4 tests
    - `python -m pytest -q` passes the full suite
  </acceptance_criteria>
  <behavior>
    - Test 1 (existing): default `persist is True` passes
    - Test 2 (existing): explicit `persist=False` passes
    - Test 3 (existing): `from_dict` without a `persist` key defaults to True
    - Test 4 (existing): persist=False system message visible in to_llm_messages
  </behavior>
  <action>Edit `context/message.py`: add `persist: bool = True` as the last dataclass field (after `tool_call_id`). Do NOT read `persist` from the dict in `from_dict` — stored events are all persisted by definition (to_events never writes the key; research Pitfall: "stored events should never carry the key"). Leave `__post_init__` and all other behavior untouched. The field must default True so every existing construction site (agent/core.py, context.py, tests) is backward compatible.</action>
  <verify>
    <automated>python -m pytest tests/test_persist.py -x</automated>
  </verify>
  <done>All 4 contract tests pass; `context/message.py` contains `persist: bool = True`; full suite `python -m pytest -q` still green at 103+ tests</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Session memory → JSONL file | Skill body content crosses this boundary during `store.save()` — must be blocked for `persist=False` messages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information Disclosure | `Message` / `Session.to_events()` | mitigate | `persist=False` flag on skill-body messages; `to_events()` filter (Plan 13-02) enforces the boundary; guard tests assert JSONL never contains skill content (ASVS V9) |
| T-13-02 | Tampering | `Session.mark_saved()` / `unpersisted_events()` | mitigate | Index counts persisted messages only — saves stay aligned, no lost/duplicated events (Plan 13-02 round-trip tests) |
</threat_model>

<verification>
- `python -m pytest tests/test_persist.py -x` — 4 contract tests green
- `python -m pytest -q` — full suite green (103 existing + 4 new)
- `Message.persist` defaults True (no existing caller breaks)
</verification>

<success_criteria>
- `context/message.py` has `persist: bool = True` (last field, defaulted)
- `tests/test_persist.py` exists with 4 passing tests covering default, explicit opt-out, from_dict compat, and LLM visibility
- All existing tests still pass — backward compatibility proven
- `Message.from_dict` ignores the `persist` key (stored events always persist=True)
</success_criteria>

<output>
After completion, create `.planning/phases/13-context-plumbing-persist-fix/13-01-SUMMARY.md`
</output>

---
phase: 13-context-plumbing-persist-fix
plan: 03
type: execute
wave: 3
depends_on: ["13-context-plumbing-persist-fix-02"]
files_modified: [session/models.py, context/context.py, tests/test_persist.py]
autonomous: true
requirements: [ACT-03, ACT-04, ACT-05]
must_haves:
  truths:
    - "A conversation that crosses the summarization threshold keeps its loaded skill bodies intact — the system-role exemption at context.py:88 is honored"
    - "Loaded skill bodies persist in memory for the entire session and vanish on /new or session close — session-scoped state via non-serialized Session fields"
  artifacts:
    - path: "session/models.py"
      provides: "Session.skill_state non-serialized field (session-scoped loaded-skill tracking)"
      contains: "skill_state"
    - path: "context/context.py"
      provides: "Summarization-exemption documentation at the system-role filter"
      contains: "skill body"
    - path: "tests/test_persist.py"
      provides: "Exemption + session-scoped proof (summarize-survive, skill_state never serialized)"
      min_lines: 100
  key_links:
    - from: "session/models.py"
      to: "context/context.py"
      via: "system-role skill body messages flow through to_llm_messages; context.py:88 exempts role==system from to_summarize"
      pattern: "role != \"system\""
    - from: "session/models.py"
      to: "session/store.py"
      via: "to_snapshot_meta() explicit-copy omits skill_state — JSONL stays clean"
      pattern: "skill_state"
---

<objective>
Complete the session-scoped contract: add the non-serialized `Session.skill_state` field and document the summarization exemption for system-role skill bodies (ACT-03/04/05 final proof).

Purpose: ACT-03 (persist in session memory), ACT-04 (survive summarization), and ACT-05 (never in JSONL) converge here. The system-role exemption already exists at `context/context.py:88` — this plan documents the contract (not reimplemented) and proves it with a summarization-crossing test. `skill_state` follows the Phase 12 non-serialized-by-construction pattern (`skill_manifest`), guarded by a regression test.
Output: `Session.skill_state` field + exemption documentation + final tests proving all three requirements end-to-end.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-context-plumbing-persist-fix/13-RESEARCH.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from the codebase — use directly, no exploration needed. -->

From session/models.py (current state, after Plans 13-01/13-02):
```python
@dataclass
class Session:
    id: str
    system_prompt: str
    title: str | None = None
    created_at: datetime = field(default_factory=datetime.now)
    updated_at: datetime = field(default_factory=datetime.now)
    metadata: dict = field(default_factory=dict)
    skill_manifest: str | None = None   # Phase 12 — non-serialized by construction
    _context: ConversationContext | None = None
    _last_saved_count: int = 0

def to_snapshot_meta(self) -> dict:     # L108-117 — explicit-copy ONLY these fields:
    # type, id, title, created_at, updated_at, system_prompt, metadata
    # → any other dataclass field (skill_manifest, skill_state) is non-serialized by construction

def to_events(self) -> list[dict]:      # now filters `if not msg.persist: continue`
```

From context/context.py — the exemption that must be documented (L88):
```python
to_summarize = [
    m for m in self._messages
    if m.role != "system" and id(m) not in recent_ids
]
```
</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: RED — write exemption + session-scoped tests</name>
  <files>tests/test_persist.py</files>
  <read_first>tests/test_persist.py (append after Plans 13-01/13-02's 8 tests), tests/test_summarization.py (ConversationContext threshold patterns), context/context.py (summarization exemption at L88)</read_first>
  <acceptance_criteria>
    - tests 9-11 appended: summarize-survive, skill_state not in snapshot meta/JSONL, skill_state fresh per session
    - `python -m pytest tests/test_persist.py -x` — tests 1-8 pass, tests 9-11 fail for the correct reason (skill_state attribute absent)
  </acceptance_criteria>
  <behavior>
    - Test 9: A system-role `persist=False` skill body survives `_maybe_summarize()` — after summarization triggers, the body text is still in `ctx.to_llm_messages()` (ACT-04)
    - Test 10: `Session.skill_state` set to a loaded-skill dict is NOT in `to_snapshot_meta()` and NOT in the saved JSONL file (ACT-05)
    - Test 11: A fresh session created after close has empty/default `skill_state` — session-scoped state does not leak across sessions (ACT-03)
  </behavior>
  <action>Append tests 9-11 to `tests/test_persist.py`. Test 9: build a `ConversationContext(count_tokens=lambda t: len(t.split()), token_limit=60, summarize_fn=mock_summarize, summarize_threshold=0.75, keep_recent_exchanges=0)` (mirror `tests/test_summarization.py` patterns), add a `Message(role="system", content="SKILL BODY", persist=False)`, then add enough user/assistant messages to cross the threshold; assert the skill body content remains in `to_llm_messages()` after summarization fired. Test 10: `session.skill_state = {"demo-greeter": {"loaded": True}}`, assert `"skill_state" not in session.to_snapshot_meta()`, save via `JSONLSessionStore(tempfile.mkdtemp())`, assert `"skill_state" not in file_text`. Test 11: `Session.create(...)` twice, set skill_state on the first, assert the second's `skill_state == {}`. Run — tests 9-11 must FAIL (skill_state attribute missing).</action>
  <verify>
    <automated>python -m pytest tests/test_persist.py -x (expect RED on tests 9-11, GREEN on 1-8)</automated>
  </verify>
  <done>Three tests appended; failing for the correct reason (skill_state attribute absent)</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: GREEN — add skill_state field + document exemption</name>
  <files>session/models.py, context/context.py</files>
  <read_first>session/models.py (file being modified — see current state in plan context), context/context.py (L85-89 to_summarize filter — file being modified), tests/test_persist.py (tests to satisfy)</read_first>
  <acceptance_criteria>
    - `session/models.py` contains `skill_state: dict = field(default_factory=dict)` immediately after `skill_manifest`
    - `to_snapshot_meta()` does NOT include skill_state (explicit-copy unchanged)
    - `context/context.py` contains a comment at the to_summarize comprehension naming the system-role exemption and ACT-04
    - No logic changed in either file (diff is field + comment only)
    - `python -m pytest tests/test_persist.py -x` passes all 11 tests
    - `python -m pytest -q` passes the full suite
  </acceptance_criteria>
  <behavior>
    - Test 9 (existing): skill body survives summarization (exemption honored)
    - Test 10 (existing): skill_state never in snapshot meta or JSONL
    - Test 11 (existing): skill_state is fresh per session
  </behavior>
  <action>Edit `session/models.py`: add `skill_state: dict = field(default_factory=dict)` immediately after `skill_manifest: str | None = None` (before `_context`). Do NOT add it to `to_snapshot_meta()` — the explicit-copy pattern keeps it non-serialized by construction (same as `skill_manifest`). Then edit `context/context.py`: at the `to_summarize` comprehension (L85-89), add an inline comment documenting the contract: `# System-role messages (summary + loaded skill bodies, D-12) are exempt from summarization — bodies persist for the session (ACT-04).` Do NOT change any logic in either file.</action>
  <verify>
    <automated>python -m pytest tests/test_persist.py -x; then python -m pytest -q</automated>
  </verify>
  <done>All 11 persist tests pass; `session/models.py` has `skill_state: dict = field(default_factory=dict)`; `context/context.py` has the exemption comment; full suite green (103 + 11)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Session memory → LLM (via summarization) | Skill body content must survive compaction intact — the system-role exemption is the guarantee |
| Session state → JSONL | `skill_state` must never cross into the session file — explicit-copy omission is the guarantee |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | Information Disclosure | `ConversationContext._maybe_summarize()` | mitigate | System-role exemption at L88 keeps skill bodies out of the summary payload (which IS serialized); test 9 proves survival, comment documents the contract (ASVS V9) |
| T-13-01 | Information Disclosure | `Session.to_snapshot_meta()` | mitigate | `skill_state` omitted from explicit-copy — non-serialized by construction; test 10 asserts absence from snapshot and JSONL (reinforces the Plan 13-02 filter) |
</threat_model>

<verification>
- `python -m pytest tests/test_persist.py -x` — 11 tests green
- `python -m pytest -q` — full suite green (103 + 11)
- `Session.skill_state` defaults to a fresh empty dict per session
</verification>

<success_criteria>
- `session/models.py` has `skill_state: dict = field(default_factory=dict)` — non-serialized by construction
- `context/context.py` documents the system-role summarization exemption at the filter
- Test 9 proves skill bodies survive summarization (ACT-04)
- Tests 10-11 prove session-scoped state: never in JSONL, fresh per session (ACT-03/05)
</success_criteria>

<output>
After completion, create `.planning/phases/13-context-plumbing-persist-fix/13-03-SUMMARY.md`
</output>

---
phase: 11-session-popup-and-panel-layout
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [tui-ink/src/types.ts, tui-ink/src/bridge/rpc-client.ts, tui-ink/src/store/agent-store.ts]
autonomous: true
requirements: [D-10]
user_setup: []

must_haves:
  truths:
    - "The TUI can request a session's history over RPC (sessions.get)"
    - "The store can ingest loaded history into the conversation view"
    - "History messages are typed (role user|assistant, content) and non-streaming"
  artifacts:
    - path: "tui-ink/src/types.ts"
      provides: "SessionMessage + SessionHistoryResponse contracts"
      contains: "export interface SessionMessage"
    - path: "tui-ink/src/bridge/rpc-client.ts"
      provides: "getSessionHistory RPC call"
      contains: "getSessionHistory"
    - path: "tui-ink/src/store/agent-store.ts"
      provides: "loadConversation store action"
      contains: "loadConversation"
  key_links:
    - from: "tui-ink/src/bridge/rpc-client.ts"
      to: "sessions.get RPC"
      via: "this.request(\"sessions.get\", { session_id })"
      pattern: "sessions.get"
    - from: "tui-ink/src/store/agent-store.ts"
      to: "tui-ink/src/types.ts"
      via: "loadConversation(messages: SessionMessage[])"
      pattern: "SessionMessage"
---

<objective>
TUI contracts: history types + RPC client method + store ingestion

Purpose: Create the TypeScript contracts the SessionPicker (Plan 03) and layout (Plan 04) build against — interface-first ordering. The `sessions.get` RPC shape is locked by UI-SPEC §10: `request("sessions.get", { session_id })` returning `{ messages: { role, content }[] }`. Runs in Wave 1 parallel with the backend plan (Plan 01) because it implements the same wire contract from the client side — zero file overlap.
Output: `tui-ink/` compiles with the new history-loading surface; `npm run typecheck` passes with 0 errors.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-session-popup-and-panel-layout/11-CONTEXT.md
@.planning/phases/11-session-popup-and-panel-layout/11-RESEARCH.md
@.planning/phases/11-session-popup-and-panel-layout/11-UI-SPEC.md
@tui-ink/src/types.ts
@tui-ink/src/bridge/rpc-client.ts
@tui-ink/src/store/agent-store.ts

<interfaces>
<!-- Verified against installed code. Executor must NOT re-explore. -->

From tui-ink/src/types.ts:
- `SessionSummary { id: string; title: string | null; created_at: string; updated_at: string; message_count: number }`
- `Message { id: string; role: "user" | "assistant" | "notice" | "error"; content: string; timestamp: number; isStreaming?: boolean; truncated?: boolean }`

From tui-ink/src/bridge/rpc-client.ts:
- `async request(method: string, params?: Record<string, unknown>): Promise<unknown>` — JSON-RPC over stdio, 30s timeout (L87-101)
- Existing method to mirror (L116-121): `switchSession(sessionId)` → `request("sessions.switch", { session_id })`
- Note: adapter returns `{"error": "..."}` dicts (not JSON-RPC errors) for missing/invalid params — the client MUST detect and throw

From tui-ink/src/store/agent-store.ts:
- Store actions: `setActiveSession(id)`, `resetConversation()` (clears conversation/toolCalls/toolCallCount/status/error, L195-202), `setSessions(sessions)`
- Internal helpers: `const nextId = () => \`msg_${++msgCounter}\``, `const now = () => Date.now()`
- `AgentActions` interface at L15-37 — every new action MUST be declared there
</interfaces>

<tasks>

<task type="auto">
  <name>task 11-02-01: history payload types</name>
  <files>tui-ink/src/types.ts</files>
  <read_first>
    - tui-ink/src/types.ts (full — 93 lines; add after SessionSummary, keep existing types untouched)
  </read_first>
  <action>
    Add after the `SessionSummary` interface (after L7):
    ```typescript
    export interface SessionMessage {
      role: "user" | "assistant"
      content: string
    }

    export interface SessionHistoryResponse {
      messages: SessionMessage[]
    }
    ```
    Do NOT add tool/system roles — UI-SPEC §10 locks ingest mapping to `user`/`assistant` only (system summaries and tool results are skipped at ingest time; the backend may send extra keys per message, which TS structural typing tolerates on read).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `tui-ink/src/types.ts` contains `export interface SessionMessage` and `export interface SessionHistoryResponse` (grep matches)
    - `npm run typecheck` exits 0 (run in `tui-ink/`)
  </acceptance_criteria>
  <done>
    SessionMessage/SessionHistoryResponse exported and typecheck-clean
  </done>
</task>

<task type="auto">
  <name>task 11-02-02: RpcClient.getSessionHistory</name>
  <files>tui-ink/src/bridge/rpc-client.ts</files>
  <read_first>
    - tui-ink/src/bridge/rpc-client.ts (imports L1-7, switchSession L116-121 as the pattern to mirror)
    - tui-ink/src/types.ts (SessionMessage added in task 1)
  </read_first>
  <action>
    Two edits:

    1. Update the type import (L7): `import type { EventPayload, SessionMessage, SessionSummary } from "../types.js"`

    2. Add after `deleteSession` (L133):
    ```typescript
    async getSessionHistory(sessionId: string): Promise<SessionMessage[]> {
      const result = (await this.request("sessions.get", { session_id: sessionId })) as
        | { messages: SessionMessage[] }
        | { error: string }
      if ("error" in result) {
        throw new Error(result.error)
      }
      return result.messages
    }
    ```
    The `"error" in result` branch is required: the backend adapter returns `{"error": "..."}` dicts for missing/invalid/not-found params rather than JSON-RPC error responses — without the check, the picker would receive `{error}` where messages are expected.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `tui-ink/src/bridge/rpc-client.ts` contains `getSessionHistory` with `"sessions.get"` and `{ session_id: sessionId }` (grep matches)
    - `tui-ink/src/bridge/rpc-client.ts` contains `"error" in result` inside getSessionHistory (grep matches)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>
    getSessionHistory typed, error-aware, and typecheck-clean
  </done>
</task>

<task type="auto">
  <name>task 11-02-03: store loadConversation action</name>
  <files>tui-ink/src/store/agent-store.ts</files>
  <read_first>
    - tui-ink/src/store/agent-store.ts (AgentActions interface L15-37, resetConversation L195-202, nextId/now helpers L10-13, imports L1-8)
  </read_first>
  <action>
    Three edits:

    1. Import `SessionMessage` — extend the type import (L2-8) with `SessionMessage`.

    2. Declare in `AgentActions` (after `resetConversation`, L34):
    ```typescript
    loadConversation: (messages: SessionMessage[]) => void
    ```

    3. Implement after `resetConversation` (L202). Mirror resetConversation's resets (research Pitfall 4 — ingested history must not inherit prior tool/streaming state), and seed the conversation with non-streaming messages:
    ```typescript
    loadConversation: (messages) =>
      set({
        conversation: messages.map((m) => ({
          id: nextId(),
          role: m.role,
          content: m.content,
          timestamp: now(),
        })),
        toolCalls: [],
        toolCallCount: 0,
        status: "idle",
        error: null,
      }),
    ```
    Timestamps are `Date.now()` at ingest (UI-SPEC §10; ordering comes from array order, not timestamps). No `isStreaming`/`truncated` fields — ingested history is static (research Pitfall 4: the `token` event handler checks `lastMsg.role !== "assistant"` on the conversation tail; non-streaming messages keep that logic correct).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `tui-ink/src/store/agent-store.ts` contains `loadConversation: (messages: SessionMessage[]) => void` in AgentActions (grep matches)
    - `tui-ink/src/store/agent-store.ts` contains `conversation: messages.map((m) => ({` (grep matches)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>
    loadConversation declared and implemented; ingest resets tool/streaming state; typecheck clean
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RPC response → TUI store | Untrusted/undefined history payloads cross into the store |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Information disclosure | `getSessionHistory` | accept | Local single-user tool; the RPC subprocess is spawned by the TUI itself (same trust domain). No new exposure vs existing `listSessions` |
| T-11-05 | Tampering | `getSessionHistory` result shape | mitigate | `"error" in result` discrimination before the cast to `{ messages }` — a backend `{error}` dict is thrown as `Error`, never fed to the store as conversation data |
| T-11-06 | DoS | `loadConversation` with oversized arrays | accept | History bounded by real session files; Ink renders via the same `MessageCard` path already used for live conversation |
</threat_model>

<verification>
- `npm run typecheck` (in `tui-ink/`) — 0 errors
- Grep gates: `SessionMessage`, `getSessionHistory`, `"sessions.get"`, `loadConversation`
</verification>

<success_criteria>
- types.ts exports the history contracts
- rpc-client.ts can fetch history with error discrimination
- store can ingest history with correct state resets
- `npm run typecheck` passes with 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-session-popup-and-panel-layout/11-02-SUMMARY.md`
</output>
